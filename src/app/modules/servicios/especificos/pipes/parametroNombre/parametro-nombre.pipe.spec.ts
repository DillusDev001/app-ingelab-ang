import { ParametroNombrePipe } from './parametro-nombre.pipe';

describe('ParametroNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new ParametroNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
